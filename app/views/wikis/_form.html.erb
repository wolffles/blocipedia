<%= form_for wiki do |f| %>
  <div class="form-group">
    <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
  </div>
  <div class="form-group">
    <%= f.label :body %>
    <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post body" %>
  </div>
  <% if current_user.admin? || current_user.premium? # dont need this because a user shouldn't able to edit someone elses wiki without being a collaborator && current_user.id == wiki.user_id %>
    <div class="form-group">
      <%= f.label :private, class: 'checkbox' do %>
        <%= f.check_box :private %> Private wiki
      <% end %>
    </div>
  <% end %>
  <!-- <dif class="form-group">
    <% f.label :collaborators %>
    <% f.text_field :collaborators, class: 'form-control', placeholder: "email seperated by space example: sample@mail.com AnotherSample@mail.com third@mail.com" %> -->
  <!-- form to add collaborators here with example of how to do it. find user by email. add the id number to collaborator push email to wiki.collaborator if email exists in users -->
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
  <% end %>
